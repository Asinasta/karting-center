openapi: 3.1.0
info:
  title: Apex Common Models
  version: 0.1.0
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum:
            - slot_full
            - double_booking
            - slot_cancelled
            - slot_started
            - already_cancelled
            - invalid_code
            - rate_limit
            - phone_already_used
            - unauthorized
            - forbidden
            - not_found
            - validation_error
            - already_rated
            - rating_not_eligible
            - server_error
        message:
          type: string
        details:
          type: object
          additionalProperties: true
    Money:
      type: object
      required: [amount, currency]
      properties:
        amount:
          type: integer
          description: Amount in kopecks
        currency:
          type: string
          enum: [RUB]
    Pagination:
      type: object
      properties:
        limit:
          type: integer
        offset:
          type: integer
        total:
          type: integer
    RetryAfter:
      type: object
      required: [retry_after]
      properties:
        retry_after:
          type: integer
          description: Seconds until retry is allowed
          minimum: 0
