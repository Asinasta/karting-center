openapi: 3.1.0
info:
  title: Apex Marshals API
  version: 0.1.0
paths:
  /marshals:
    get:
      operationId: listMarshals
      summary: List marshals for filters
      responses:
        '200':
          description: Marshals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './models.yaml#/components/schemas/Marshal'
