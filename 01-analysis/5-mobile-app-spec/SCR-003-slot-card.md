# SCR-003 — Карточка заезда

## API

- `getSlot`: `GET /slots/{slotId}` без авторизации

## UI

Дата/время, конфигурация трассы, маршал, цена, свободные места, прокатная экипировка, место сбора, карта.

## CTA

`Записаться` ведёт на SCR-004, если слот доступен. Если пользователь не авторизован, сначала открывается OTP-вход, затем возврат к выбранному слоту.

## Применяемые логики

LOGIC-002, LOGIC-003, LOGIC-006, LOGIC-008.
